{
  "author": "",
  "description": "",
  "keywords": [],
  "license": "ISC",
  "main": "dist/index",
  "name": "@opoint/core",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "rm -rf dist/* && tsc -p tsconfig.pkg.json",
    "prepack": "cp ../../README.md ./ && cp ../../LICENSE ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "version": "0.0.16",
  "dependencies": {
    "@opoint/types": "file:../types"
  }
}
